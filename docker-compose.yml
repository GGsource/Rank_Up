version: "3.8"

services:
    app:
        build: .
        ports:
            - "5173:5173"
        volumes:
            - .:/app # Sync the local files
            - /app/node_modules # Keep the container's node_modules
